import { describe, it, expect } from "bun:test";

import Header from "./Header";

describe("A test of the header components", () => {
  it("Definately works?", () => {
    expect(true).toBe(true);
  });
});
